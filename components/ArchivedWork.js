import { Flex, Grid, Text, Link } from './system'
import { Layout } from './bridge'
import { Project } from './SelectWorkEls'
import { Row } from './ContentViews'

export default function ArchivedWork(props) {

	<Row
		title={data.title}
		desc={data.title}
		tag={data.title}
	/>
}